import styled from "styled-components";
import {myTheme} from "../Theme.styled";

export const StyledDot = styled.div`
  width: 4px;  /
  height: 4px; 
  background-color: ${myTheme.colors.lightGrey}; 
  border-radius: 50% / 100%;
`